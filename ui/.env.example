# Copy to ui/.env.local
# true = build the whole interface against mocks, no backend needed.
# Also the emergency demo fallback if inference dies.
# NEXT_PUBLIC_ prefix is required — without it the value is undefined in the browser.
NEXT_PUBLIC_USE_MOCKS=false
