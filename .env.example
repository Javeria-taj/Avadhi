# Copy to .env and adjust. Never commit .env.
BACKEND=mlx
MODEL_PATH=./models/gemma-4b
ASR_BACKEND=whisper
ASR_MODEL=small
LANGUAGE=kn
# true = run the whole pipeline with no model. Use for UI work and as the
# emergency demo fallback.
MOCK_MODE=false
